#!/usr/bin/env bash

set -euo pipefail

# Fetch all release tags from GitHub
releases=$(curl -s "https://api.github.com/repos/cartridge-gg/paymaster/releases" | \
  grep '"tag_name":' | \
  sed -E 's/.*"v([^"]+)".*/\1/' | \
  sort -V)

echo "$releases"
